[project]
name = "agent-skills"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "prek>=0.3.4,<0.4",
    "skills-ref>=0.1.1",
]

[tool.ruff]
target-version = "py314"
line-length = 100

[tool.ty.environment]
python-version = "3.14"
